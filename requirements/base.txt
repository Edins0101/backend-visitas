# API
fastapi>=0.110
uvicorn[standard]>=0.27

# Validación / config
pydantic>=2.6
pydantic-settings>=2.2

# DB (Postgres sync)
sqlalchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13

# Seguridad (hash, si manejas passwords/códigos)
passlib[bcrypt]>=1.7

# HTTP client (para consumir el endpoint de reconocimiento facial)
httpx>=0.27

# Calidad / tests
pytest>=8.0
ruff>=0.3

# Lectura de enviroments
python-dotenv==1.0.1